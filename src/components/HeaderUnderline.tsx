import styled from "@emotion/styled";

const HeaderUnderline = styled('div')({
    width: '100px',
    height: '3px',
    marginLeft: 0,
    marginRight: 0,
    marginBottom: '80px',
    padding: 0,
    background: 'var(--navy)',
})

export default HeaderUnderline;